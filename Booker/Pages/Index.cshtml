@page
@model IndexModel
@{
    ViewData["Title"] = Model.IsMaintenanceMode ? "TextBooker – Wracamy we wrześniu!" : "TextBooker";
    var partialName = Model.IsMaintenanceMode ? "_MaintenanceContent" : "_CatalogContent";
    var partialModel = Model.IsMaintenanceMode ? null : Model;
}

<partial name="@partialName" model="partialModel" />
